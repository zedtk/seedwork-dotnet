# nuget-publish-plugin

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build nuget-publish-plugin` to build the library.

## Running unit tests

Run `nx test nuget-publish-plugin` to execute the unit tests via [Jest](https://jestjs.io).
