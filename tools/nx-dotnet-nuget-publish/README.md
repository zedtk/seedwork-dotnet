# nx-dotnet-nuget-publish

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build nx-dotnet-nuget-publish` to build the library.

## Running unit tests

Run `nx test nx-dotnet-nuget-publish` to execute the unit tests via [Jest](https://jestjs.io).
